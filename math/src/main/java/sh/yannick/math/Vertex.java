package sh.yannick.math;

/**
 * A vertex in a graph.
 *
 * @author Yannick Kirschen
 * @since 1.0.0
 */
public interface Vertex {
    /**
     * @return label of the vertex used for identifying a vertex in the graph
     */
    String getLabel();
}
